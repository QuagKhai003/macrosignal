# ── workflow / process files (local-only, never committed) ──
CLAUDE.md
docs/
.claude/
*.plan.md
*.notes.md
scratch/
.notes/
product_info/

# ── secrets ──
.env

# ── python ──
.venv/
__pycache__/
*.pyc

# ── dev/test artifacts ──
coverage/
.cache/
*.log
logs/
# tests/    ← stays committed (user chose: tests are product code)

# ── local data (rebuildable from sources) ──
signals.db
signals.db-journal
signals.db-wal
signals.db-shm
