# Signal registry — THE admission-test config (product doc §11, build plan Phase 0).
# A signal enters the machine only if ALL five admission questions pass:
#   1. a number, not a vibe          → it is a numeric series stored in observations
#   2. free + fixed schedule         → source_url + schedule fields
#   3. 10+ years of history          → history_start (validated vs the run date)
#   4. one-sentence causal story     → causal_sentence
#   5. named elite watchers          → elite_watcher
# Groups (product doc §11): A positioning, B smart-money paperwork, C regulatory,
# D physical world, E company behavior, F market vital signs, G retail, H elite liquidity.
# pub_lag_days encodes the publication lag (as-of discipline; used by the Phase 5 replay).
# Validated by src/registry.py; mirrored into the signals.db `series` table each run.

signals:
  - series_id: net_liquidity
    name: Net liquidity (Fed balance sheet - Treasury account - reverse repo)
    group: H
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=WALCL  # derived with WTREGEN and RRPONTSYD
    schedule: weekly
    pub_lag_days: 1
    history_start: 2015-01-01
    window: rolling10y
    series_codes: [WALCL, WTREGEN, RRPONTSYD]
    causal_sentence: Central banks control the water level of the market pool, so rising net liquidity lifts asset prices before the crowd notices the tide.
    elite_watcher: Michael Howell (CrossBorder Capital), Stanley Druckenmiller, Michael Hartnett (BofA)

  - series_id: cot_managed_money
    name: CFTC COT Managed Money net positioning (gold, WTI, 10-yr T-note, EUR, corn)
    group: A
    source: CFTC
    source_url: https://www.cftc.gov/dea/newcot/f_disagg.txt
    schedule: weekly
    pub_lag_days: 3  # Tuesday data, Friday release
    history_start: 2006-06-13
    window: rolling3y
    markets:  # series_id -> CFTC contract code + report. Commodities live in the
      # Disaggregated report (party = Managed Money); financials are NOT in it —
      # they live in the TFF report (party = Leveraged Funds). Spec-gap fix 2026-07-18.
      cot_gold: {code: "088691", report: disagg}
      cot_wti: {code: "067651", report: disagg}
      cot_ust10y: {code: "043602", report: tff}
      cot_eur: {code: "099741", report: tff}
      cot_corn: {code: "002602", report: disagg}
    causal_sentence: Trend-chasing speculators are right mid-trend and wrong at turns, so their positioning extremes mark old crowded trends and their abandonment marks early zones.
    elite_watcher: Managed-futures desks (Man AHL, AQR), veteran COT analysts (Larry Williams)

  - series_id: spine_prices
    name: Daily closes for the 5 spine markets (GC, CL, ZN, EURUSD, ZC) feeding the 200-day momentum gate
    group: F
    source: Yahoo
    source_url: https://query1.finance.yahoo.com/v8/finance/chart/GC=F
    schedule: daily
    pub_lag_days: 0
    history_start: 2000-01-01
    window: sma200
    markets:  # series_id -> Yahoo symbol (stooq is JS-challenge-walled; decision 2026-07-18)
      price_gold: GC=F
      price_wti: CL=F
      price_ust10y: ZN=F
      price_eur: EURUSD=X
      price_corn: ZC=F
      price_wilshire: ^W5000  # weather gauge 2 numerator (FRED dropped WILL5000PR)
    causal_sentence: Investors underreact to news and then herd, so a price above its own 200-day average tends to keep drifting the same way.
    elite_watcher: Trend-following funds (AQR, Man AHL, Winton) running the century-validated strategy

  - series_id: real_yields
    name: 10-year TIPS real yield, FRED DFII10 (gold's engine + driver-alive correlation)
    group: H
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=DFII10
    schedule: daily
    pub_lag_days: 1
    history_start: 2003-01-02
    window: rolling10y
    series_codes: [DFII10]
    causal_sentence: Gold pays no interest, so when the real yield on safe bonds falls the penalty for holding gold disappears and its price rises.
    elite_watcher: Macro funds' gold desks (Bridgewater), World Gold Council research

  - series_id: oil_inventories
    name: US commercial crude oil stocks, EIA weekly (oil's engine)
    group: D
    source: EIA
    source_url: https://api.eia.gov/v2/seriesid/PET.WCESTUS1.W
    schedule: weekly
    pub_lag_days: 5  # week ends Friday, report lands the following Wednesday
    history_start: 1982-08-20
    window: same_week_5y_avg
    causal_sentence: Crude stocks below their seasonal norm mean demand is outrunning supply, which lifts oil prices before the shortage makes headlines.
    elite_watcher: Every oil trading desk (the weekly EIA report is oil's scheduled market-mover), Pierre Andurand
    series_codes: [PET.WCESTUS1.W]

  - series_id: news_heat
    name: Theme news volume + signed sentiment (the crowding meter, F6/F7)
    group: G
    source: GDELT
    source_url: https://api.gdeltproject.org/api/v2/doc/doc
    schedule: weekly
    pub_lag_days: 0
    history_start: 2017-01-01  # DOC 2.0 API coverage
    window: fixed_threshold  # loud >3.0x, un-loud <2.0x, quiet <1.5x (F6)
    causal_sentence: Noise is the last stage of a move, so when everyone is already talking about a theme, few buyers remain who have not bought.
    elite_watcher: Contrarian PMs everywhere; the BofA FMS "most crowded trade" question measures the same herd
    # keyed fallback when api.gdeltproject.org refuses (user decision
    # 2026-07-18): fills HEADLINES only — Cloud counts story clusters, the
    # Project counts articles; mixing scales would corrupt the F6 ratio.
    cloud_fallback:
      url: https://gdeltcloud.com/api/v2/stories
      # free tier = 100 requests/month/key; rotate through keys, advancing to
      # the next when one is quota-exhausted (429 / PLAN_REQUIRED)
      key_envs: [GDLTE_CLOUD_API_KEY, GDLTE_CLOUD_API_KEY_2,
                 GDLTE_CLOUD_API_KEY_3, GDLTE_CLOUD_API_KEY_4,
                 GDLTE_CLOUD_API_KEY_5]
    # GDELT query per theme — deterministic, editable config (build plan Phase 4)
    themes:
      gold: '"gold price" OR "gold rally" OR "gold market"'
      wti: '"oil price" OR "crude oil" OR "OPEC"'
      ust10y: '"treasury yields" OR "bond market selloff" OR "10-year yield"'
      eur: '"euro exchange rate" OR "euro dollar" OR "ECB rate"'
      corn: '"corn price" OR "corn crop" OR "grain prices"'
    # the caged LLM (decision 2026-07-18: NIM primary, OpenRouter fallback).
    # Tried in order; a provider without its key is skipped; mid-run failure
    # fails over to the next (each label's audit row stamps provider:model).
    providers:
      # both chosen models REASON before answering -> generous token budgets;
      # the parser accepts exactly-one-distinct-label replies either way
      - {name: nim, url: "https://integrate.api.nvidia.com/v1/chat/completions",
         key_env: NIM_API_KEY, model: stepfun-ai/step-3.7-flash,
         max_tokens: 2000}
      - {name: openrouter, url: "https://openrouter.ai/api/v1/chat/completions",
         key_env: OPENROUTER_API_KEY,
         model: "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free",
         max_tokens: 2000}
    prompt_version: v1.0

  - series_id: us_dollar
    name: US broad dollar index, FRED DTWEXBGS (weather/EM penalty gauge, H2)
    group: H
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=DTWEXBGS
    schedule: daily
    pub_lag_days: 1
    history_start: 2006-01-02
    window: rolling10y
    series_codes: [DTWEXBGS]
    causal_sentence: A strong and rising dollar strangles emerging markets and commodities because their debts and goods are priced in dollars, so it drains those trades before the crowd reacts.
    elite_watcher: Every EM and commodity macro desk; the IMF's dollar-liquidity monitors

  - series_id: inflation_expectations
    name: 10-year breakeven inflation, FRED T10YIE (the bond engine's driver)
    group: F
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=T10YIE
    schedule: daily
    pub_lag_days: 1
    history_start: 2003-01-02
    window: rolling10y
    series_codes: [T10YIE]
    causal_sentence: Bond prices rise when the inflation the market expects falls, because fixed coupons are worth more when money holds its value, so falling breakevens lift Treasuries.
    elite_watcher: Bond desks and the Fed itself (breakevens are a stated policy input)

  - series_id: us_ez_rate_diff
    name: US minus Eurozone short-rate differential, FRED DGS2 - ECBDFR (the euro engine's driver)
    group: H
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=DGS2
    schedule: daily
    pub_lag_days: 1
    history_start: 2003-01-02
    window: rolling10y
    series_codes: [DGS2, ECBDFR]  # derived = DGS2 - ECBDFR (src/spine)
    causal_sentence: A currency strengthens when its interest-rate advantage grows, so when the US-Eurozone rate gap narrows the euro gains as capital rotates back toward it.
    elite_watcher: FX carry desks worldwide; the ECB and Fed both watch the differential

  - series_id: corn_stocks
    name: US corn grain stocks, USDA NASS quarterly (corn's engine)
    group: D
    source: NASS
    source_url: https://quickstats.nass.usda.gov/api
    schedule: quarterly
    pub_lag_days: 50  # quarterly Grain Stocks report lands ~7 weeks after the reference date
    history_start: 1975-01-01
    window: same_quarter_5y_avg
    causal_sentence: Corn stocks below their seasonal norm mean the crop is being consumed faster than expected, and tight grain tightens prices before the shortage is widely reported.
    elite_watcher: Every agricultural trading desk (Cargill, ADM); the USDA Grain Stocks report is a scheduled market-mover
    nass_query:  # NASS Quick Stats parameters (deterministic, editable config)
      commodity_desc: CORN
      statisticcat_desc: STOCKS
      unit_desc: BU
      agg_level_desc: NATIONAL
      source_desc: SURVEY
    # the USE side of stocks-to-use (research R1): annual production feeds the
    # implied-disappearance calculation in src/spine.derive_corn_stocks_use
    nass_production_query:
      commodity_desc: CORN
      statisticcat_desc: PRODUCTION
      unit_desc: BU
      agg_level_desc: NATIONAL
      source_desc: SURVEY
      reference_period_desc: YEAR

  - series_id: whale_berkshire
    name: Berkshire Hathaway cash position from 10-Q/10-K filings (the whale ledger)
    group: B
    source: EDGAR
    source_url: https://data.sec.gov/submissions/CIK0001067983.json
    schedule: quarterly
    pub_lag_days: 0  # pub_date = the actual EDGAR filing date (true as-of)
    history_start: 2004-01-01
    window: rolling20y  # vs the entity's own history (§5c Rule 2)
    causal_sentence: When the most patient large investor on earth hoards cash instead of buying, the market offers nothing worth buying at current prices.
    elite_watcher: Warren Buffett IS the signal; every allocator tracks his 10-Q cash line
    cik: "0001067983"
    # XBRL fact names summed for "cash" (per-segment contexts dedupe by context id)
    cash_tags: ["us-gaap:CashAndCashEquivalentsAtCarryingValue", "brka:USTreasuryBills"]
    equity_tags: ["us-gaap:EquitySecuritiesFvNi"]
    # F13 insider-cluster universe: EMPTY until the equity themes are chosen
    insider_tickers: []

  - series_id: manager_cash
    name: BofA Global Fund Manager Survey headline cash level, percent (weather gauge 1)
    group: A
    source: MANUAL  # survey is a PDF; the operator types one number per month
    source_url: https://www.bofa.com  # published mid-month via financial press
    schedule: monthly
    pub_lag_days: 0  # entered on the day it is read
    history_start: 2001-04-01
    window: fixed_threshold  # red below 4% (publisher's own trigger)
    causal_sentence: Fund managers' spare cash is dry powder, and when almost none is left there is nobody remaining to buy the next dip.
    elite_watcher: Michael Hartnett (BofA, the survey's author), every macro desk that reads the FMS
    entered_with: python manual_entry.py manager_cash <value> --date YYYY-MM-DD

  - series_id: market_valuation
    name: US market cap / GDP, the Buffett indicator (weather gauge 2 - GDP leg)
    group: F
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=GDP
    schedule: quarterly
    pub_lag_days: 120  # advance estimate lands ~4 months after the quarter's start date
    history_start: 1947-01-01
    window: rolling20y  # STANDING RULE: drifting series get rolling 20-yr, never all-history
    causal_sentence: When the whole stock market costs more than everything the country produces in a year, buyers are paying for hope, and hope-priced markets have less room to rise.
    elite_watcher: Warren Buffett (his stated favorite single measure), market strategists everywhere
    series_codes: [GDP]

  - series_id: credit_spread
    name: Corporate credit spread, Moody's Baa vs 10-yr Treasury (weather gauge 3)
    group: F
    source: FRED
    source_url: https://fred.stlouisfed.org/graph/fredgraph.csv?id=BAA10Y
    schedule: daily
    pub_lag_days: 1
    history_start: 1986-01-02
    window: rolling10y
    # BAA10Y = the working gauge (full history, uncapped). BAMLH0A0HYM2 (the
    # tech spec's HY OAS) is FRED-capped at ~3 trailing years (L-001) — it
    # keeps accumulating here so it can take over once its archive matures.
    series_codes: [BAA10Y, BAMLH0A0HYM2]
    causal_sentence: Credit investors demand extra yield the moment repayment looks shakier, so spreads widen before equity markets admit trouble.
    elite_watcher: Howard Marks (Oaktree), the Federal Reserve's financial-stability monitors
